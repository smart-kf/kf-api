jwtKey: abcdefg
debug: true

# CREATE DATABASE kf CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
db:
  driver: mysql  # mysql / postgres / sqlite ...
  dsn: "root:root@tcp(localhost:3306)/kf?charset=utf8mb4&parseTime=True&loc=PRC"

redis:
  db: 0
  address: localhost:6379
  password: ""

billConfig:
  accounts:
    - username: admin
      password: admin